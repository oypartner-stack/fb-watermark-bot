# 📡 بوت فيديوهات فيسبوك

> يراقب صفحة فيسبوك عامة، يحمّل أحدث فيديو كل X دقيقة، يضيف علامة مائية شفافة، ويرسله إلى Webhook على Make.com

---

## 🚀 خطوات النشر على Render (مجاناً)

### 1. رفع الكود على GitHub

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```

### 2. إنشاء حساب على Render
- اذهب إلى https://render.com
- سجّل الدخول بحساب GitHub

### 3. إنشاء Web Service جديد
- اضغط **New → Web Service**
- اختر مستودعك من GitHub
- اضبط الإعدادات:
  - **Environment**: Python 3
  - **Build Command**: `bash build.sh`
  - **Start Command**: `gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120`
- اضغط **Create Web Service**

### 4. استخدام التطبيق
1. افتح رابط التطبيق من Render
2. ارفع صورة العلامة المائية (PNG شفاف)
3. أدخل رابط صفحة فيسبوك العامة
4. أدخل رابط Webhook من Make.com
5. حدّد وقت الفحص (الافتراضي 10 دقائق)
6. اضغط **تشغيل البوت** ✅

---

## ⚙️ كيف يعمل التطبيق؟

```
كل X دقيقة:
  ↓
  yt-dlp يجلب معرّف + عنوان + رابط آخر فيديو
  ↓
  إذا كان فيديو جديد (غير محمّل سابقاً):
    ↓
    تحميل ملف الفيديو
    ↓
    ffmpeg يضيف العلامة المائية (أسفل اليمين)
    ↓
    إرسال الفيديو + العنوان إلى Webhook
```

---

## 📌 ملاحظات مهمة

- **الصفحة يجب أن تكون عامة (Public)**
- الفيديوهات الخاصة لن تعمل
- العلامة المائية تُضاف في الزاوية السفلية اليمنى
- عند إعادة تشغيل Render المجاني، ستُفقد العلامة المائية (يجب رفعها مجدداً)
- لتجنب هذا، يمكن وضع رابط URL لصورة ثابتة بدلاً من رفعها

---

## 🖼 كيف تعمل العلامة المائية؟

- يجب أن تكون الصورة **PNG شفافة**
- يقوم ffmpeg بتمديد الصورة لتطابق **أبعاد الفيديو كاملاً** (مناسب لـ Reels 9:16)
- الصورة تُوضع فوق الفيديو بالكامل (overlay كامل الإطار)
- الشفافية تُحافظ عليها — ما هو شفاف في الـ PNG سيبقى شفافاً فوق الفيديو

**نصيحة:** صمّم الـ PNG بنفس نسبة الفيديو (مثلاً 1080×1920 للـ Reels) مع خلفية شفافة وضع عناصرك (شعار، إطار، نص...) حيث تريد.
